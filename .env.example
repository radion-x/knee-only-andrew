# Environment Variables for Coolify Deployment

# ============================================
# CLIENT ENVIRONMENT VARIABLES
# ============================================

# Backend API URL - should point to your backend domain
VITE_SERVER_BASE_URL=https://api.yourdomain.com

# ============================================
# SERVER ENVIRONMENT VARIABLES
# ============================================

# Server Configuration
SERVER_PORT=3000
SERVER_BASE_URL=https://api.yourdomain.com
NODE_ENV=production

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# AI Service (Claude)
CLAUDE_API_KEY=your_claude_api_key_here

# Email Service (Mailgun)
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=mg.yourdomain.com
EMAIL_SENDER_ADDRESS=noreply@yourdomain.com
EMAIL_RECIPIENT_ADDRESS=doctor@yourdomain.com
BCC_EMAIL_RECIPIENT_ADDRESS=admin@yourdomain.com

# Authentication & Security
DASHBOARD_PASSWORD=your_secure_dashboard_password
SESSION_SECRET=generate_a_strong_random_32plus_character_secret_here

# CORS Configuration - Frontend domains allowed to access API
# Use comma-separated list for multiple domains
ALLOWED_ORIGINS=https://app.yourdomain.com,https://yourdomain.com

# Cookie Configuration
# Use leading dot for subdomain support: .yourdomain.com
COOKIE_DOMAIN=.yourdomain.com

# Proxy Configuration (REQUIRED for Coolify/Traefik)
TRUST_PROXY=true

# ============================================
# NOTES
# ============================================

# SESSION_SECRET: Generate with: openssl rand -base64 32
# COOKIE_DOMAIN: Must match your domain structure
#   - Single domain: yourdomain.com
#   - Subdomains: .yourdomain.com (note the leading dot)
# ALLOWED_ORIGINS: Must match exact frontend URL(s) including protocol
# TRUST_PROXY: MUST be true for Coolify deployments with Traefik
# SERVER_PORT: Changed from 3889 to 3000 for Traefik direct routing
